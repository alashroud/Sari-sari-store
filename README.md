# Tres Sari-Sari Store Inventory App

A desktop-based inventory system built with **Electron.js**, **HTML**, **CSS**, and **JavaScript**.

### Features
- Add, view, and delete products
- File-based local storage (JSON)

### Run locally
```bash
npm install
npm run dev
